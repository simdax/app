//TODO generate doc 
{
	(
		files:(
			app:{arg s; [ s.name.capitalize ++" : APP {}",'sc']},
		),
		dirs:(
			ext:(dirs:(methodOverwrite:())),
			helpSource:(),  //files:(rien:["test",'dutout'])),
			lib:()
		)
	)
}
//files:(sc:(app:),quark:{})
/*
(
files:

)

*/